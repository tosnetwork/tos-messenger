# Implementation status

This is the implementation side of the component list in `tosnetwork/tos-service-spec`
`docs/AGENT_NATIVE_MESSENGER_V1.md`. The architecture document is the roadmap; this
file records how far the code has got against it, with the package and the commit
that carries each item. Milestone names and numbering are the governing document's
and are not redefined here.

Status is deliberately conservative. A row is ✅ only when the behaviour it names is
implemented **and** tested end to end; a package that exists but whose behaviour is
not yet closed end to end is 🟡 with the gap named. Nothing in this file changes
gate status: this repository carries none and consumes no gate capacity.

This roadmap is intentionally OpenFox-only. Its completion denominator covers
the OpenFox runtime and supporting Messenger services. CLI and owner APIs are
operational authority surfaces for OpenFox, not separate product surfaces.

**Last re-audited against the code: 2026-08-21.** The first-principles choices are recorded in [`FIRST_PRINCIPLES_DECISIONS.md`](FIRST_PRINCIPLES_DECISIONS.md): canonical genesis representation, approved one-to-one construction, minimal Rust evidence consumer, no pre-study route choice, scoped Mailbox capabilities, first-contact default, single-authority rooms, and OpenMLS suite `0x0001`. A closed choice is not automatically completed implementation or freeze evidence.

## Milestones (governing numbering)

| Gate | Status | Where it stands |
|---|---:|---|
| Implemented route-independent M0 primitives (non-gate) | ✅ | Identity, envelope, journal, admission, payload codecs, action policy, authenticated owner decision path, negotiation foundations. The genesis representation and E2EE construction choices are closed; independent review and second-language evidence remain freeze gates |
| M0 protocol freeze | ⬜ | Two evidence items block the freeze directly (independent cryptographic review and second-implementation evidence). The decided raw-byte/bare-hex genesis representation is now applied to every audited candidate preimage with explicit version advances, regenerated vectors, a source audit preventing display-text regression, and a fail-closed durable-state generation marker; see [`M0_FREEZE_REVIEW.md`](M0_FREEZE_REVIEW.md). CI branch protection enforcing `verify` before merge is deferred by owner decision. The compensating evidence-bundle automation is implemented; a clean, reviewed run remains required for each candidate freeze commit |
| M0-R reachability and route decision | 🟡 | **Both collectors are working prototypes; the study itself has not been run.** UDP direct-feasibility collector (`pkg/probe.Run`): 🟡 — measures a datagram path; the NAT mapping class is derived from coordinator-signed bind reflections and a contradicting declaration is refused at verification (`reachability.VerifyTrial`), but the bind test separates only endpoint-independent from destination-dependent mappings, so the finer `address-dependent` and `symmetric` flavours stand as declarations the evidence cannot refute rather than measured classes, and a `none` declaration is never remotely credited. Filtering is measured as its own axis from coordinator-signed cold-source receipts (a second port by default, a second address when configured), which can only loosen the class — the strict class is never derived remotely. ADNL direct-establishment collector (`pkg/probe.RunADNL`): 🟡 — one session by construction, done-signalling via the coordinator, dual-family (IPv4 and IPv6) binding, and it measures past establishment: a hold window with keepalives records session survival (a pair counts only when both halves measured it), the initiator can deliberately drop and time a reconnect, and a configured tunnel relay (`cmd/tos-reachability-tunnel`) gives a failed direct phase a proxy-fallback collection path, so the report's `tunnel-first` branch is reachable from collected evidence. The v4 endpoint trial additionally signs bounded sized-echo outcomes, while the content-addressed v4 policy predeclares the required payloads (including the 8176-byte native maximum), paired sample floor and operator-balanced rate. A sized echo counts only when both halves measured it; success is their conjunction and latency the slower direction. v4 also signs exact RLDPv2 responses above the 2,000,000-byte part boundary, observable bidirectional loss after decoded progress, and completion of the original query without application retry; policy requires a three-part 4,000,001-byte interrupted response and paired operator-balanced transfer/recovery rates. `direct-first` requires survival, required sized-echo, RLDP transfer/recovery, and mobility-cell reconnect gates; `tunnel-first` requires tunnel survival, and any under-sampled load-bearing gate yields `insufficient-evidence` naming it. The in-process `tosutils-go` collector and optional `tos-native-adnl` sidecar share that signed evidence model. Still missing: exercised Wi-Fi↔mobile and other real mobility events, and real-network execution of the new RLDP phase. **Strata: family, reachability, and NAT mapping are cross-checked against coordinator-signed evidence at verify- and pair-time; filtering is evidence-only with no declared counterpart; carrier, UDP policy, mobility, endpoint class, and assistance remain self-declared, being unobservable remotely.** The report tool exits non-zero for any study that supports no route decision (`exitCode`, tested), and each cell surfaces both derived per-side filtering counts and the paired sized-echo attempts/rates/latencies its decision reads. **No study of either kind has been run.** Mobility-event execution ⬜, RLDP transfer tooling ✅ but real-study evidence ⬜, native-sidecar ADNL/RLDP tooling ✅ but real-study cross-check ⬜, finer mapping flavours measured rather than declared ⬜, the study itself ⬜ |
| M1 one-to-one transport | ⬜ | The E2EE construction is selected. Transport remains blocked on a qualifying M0-R route finding; E2EE independent-review/second-language freeze evidence is also outstanding |
| M2-T technical offline Mailbox and multi-Relay failover | 🟡 | `pkg/mailbox` implements the durable store and finalized Endpoint authority; `pkg/mailboxapi` and `tos-mailboxd` add a strict bounded service protocol, private Unix listener/client, restart/replay tests, 2-of-2 fan-out and explicit one-Relay degradation. The post-M0-R public transport binding, independently operated live failover, and operator evidence remain open |
| M2-C commercial Relay lease | 🔒 | Expansion Gate locked |
| M3 OpenFox and conversation-to-commerce integration | 🟡 | Typed commerce, mandates, budgets, durable negotiations, finalized quote reads, and a crash-safe escrow-location ledger exist. OpenFox `ed1fcc75` can run Alice, Bob, and Carol as three independently restartable OS processes, each owning one channel, cursor, transcript and private control socket over separate OpenMLS state owners and an opaque ciphertext Relay. OpenFox `c47a98e0` upgrades all three processes to the real AgentLoop with separate private durable workspaces and a credential-free deterministic provider: lab cursor advancement waits for the complete Agent turn/reply/transcript boundary, exact completed replay skips a second turn, and a deployed encrypted round produced exactly two Event-bound `openfox-agent-loop` replies that remained one each after Alice restart. Production commit `aa6fe1f7` closes the authenticated application-lease crash window, while `7fd3ac11` carries verified Event provenance through final model responses so strict production `Send` validation works end to end. The production channel independently verifies authenticated direct/room events and submits authenticated reply semantics under operator-bound routes to daemon-owned, restart-idempotent Event construction; tools and the native buyer are enforced through Messenger. Funding/wallet population, selected live transport, independent evidence, and execution integration remain open |
| M4 multi-device and private rooms | 🟡 | Device succession/fan-out and owner-authorized direct-history backfill are durable and tested; room epochs, the TOS-MLS adapter, and a pinned OpenMLS `0.8.1` process-isolated Driver for suite `0x0001` exist. Three-member founding/join/replacement/removal, encrypted bidirectional chat, no-past/no-future secrecy, exporter separation, forged-commit/AAD/replay refusal, self-update PCS, atomic ratchet/commit persistence and full restart recovery run in `make verify`. The runnable OpenFox MLS proxy now propagates controller-only removal as an old-epoch encrypted notice bound to the exact Remove Commit, persists the new epoch before later Relay items, and proves after restart that an immutable untrusted delivery roster can still hand the removed process ciphertext it cannot decrypt. Batch catch-up stops at that security boundary and reports a healthy terminal membership state instead of retrying an expected decryption failure. Canonical BasicCredential/group-id bytes and durable signed authority transfer are implemented. [`ROOM_HISTORY_POLICY.md`](ROOM_HISTORY_POLICY.md) freezes v1 as join-forward only; OpenMLS proves joiner and replacement-device no-past secrecy, while both direct-history export and import refuse room backfill. **Still missing:** independent review/second implementation and real Relay catch-up |

## Components

| Component | Status | Evidence | What is missing |
|---|---:|---|---|
| M0-R measured reachability study and route-strategy decision | 🟡 | `pkg/reachability`, `pkg/probe` (UDP, ADNL, filtering, tunnel relay, native sidecar, signed sized echo and RLDP transfer/recovery), `cmd/tos-reachability*` | The v4 policy makes direct/tunnel survival, reconnect, every predeclared paired sized-echo payload and segmented RLDP same-transfer recovery decisive; the maximum 8176-byte ADNL query is mandatory, so the route decision is no longer ping-only. Both the `tosutils-go` collector and native `tos-adnl-probe/1` now implement the canonical three-part RLDP loss/recovery phase; native-native acceptance strictly maps exact response-transfer progress, counted whole-socket suppression, original-query completion and payload digest into the signed trial. Real mobility events and independent real-network RLDP evidence remain unmeasured. Finer NAT-mapping flavours remain declarations the evidence can refute but not distinguish; filtering is surfaced without a policy threshold. The study itself — multiple operators, sites, real networks, and a qualifying native cross-check — has not been run |
| Messaging Endpoint delegation schema and verifier | ✅ | `pkg/identity`, `pkg/tosaddr` | — |
| Messaging Contact Descriptor and DHT locator profile | 🟡 | `pkg/directory`, `pkg/daemon`, [`DISCOVERY_BOOTSTRAP.md`](DISCOVERY_BOOTSTRAP.md), [`TOS_DHT_ADAPTER.md`](TOS_DHT_ADAPTER.md), [`HTTPS_OBJECT_SOURCE.md`](HTTPS_OBJECT_SOURCE.md) | The production route-neutral chain is retained in daemon config v8: explicit bounded Agent→delegation+descriptor-policy files are reread and matched to finalized commitments, then native DHT locator and hardened HTTPS descriptor/prekey operations refresh the durable device ledger. DHT bootstrap, file substitution, per-peer policy authority, network-representation conversion, lifecycle cleanup, positive vectors and adversarial decoding are tested. Still missing: live independently operated multi-node discovery evidence |
| One-to-one application-layer E2EE | 🟡 | `pkg/e2ee`, `pkg/e2ee/conformance`, [`E2EE_SUITE_DECISION.md`](E2EE_SUITE_DECISION.md) | The construction is approved and implemented, clears the fourteen-property harness, and has committed positive/adversarial wire vectors. Still partial: independent cryptographic review and second-language consumption are freeze gates, so the identifier is not frozen |
| Multi-device session and key-rotation model | 🟡 | `pkg/e2ee` (succession, sessions, fan-out, strict bundle-set publication), `pkg/eventlog` (device-local private prekeys, fixed-roster public contribution collection, public-only Endpoint aggregation, durable peer ledger and display-only history), `pkg/prekeyapi`, `pkg/signerapi`, `pkg/directory` (publication and portable fork evidence), `pkg/daemon` (refresh, publication and dedicated history consumer), `pkg/publicationops`, `pkg/localapi` (owner-only export/list), `cmd/tos-device-fork-evidence`, `cmd/tos-messenger-owner`, `pkg/admission`, [`PREKEY_PUBLICATION.md`](PREKEY_PUBLICATION.md), [`DEVICE_FORK_EVIDENCE.md`](DEVICE_FORK_EVIDENCE.md), [`DEVICE_HISTORY_SYNC.md`](DEVICE_HISTORY_SYNC.md) | The model is durable and **enforced** without centralized device secrets. Direct history pages contain only applied inbound/delivered outbound Events, use stable cursors and digest chains, are owner-authorized to a current roster Device, queue idempotently under the existing pair ratchet, and import into immutable checkpoint-reachable display state outside every execution path. A bounded owner-only read makes committed display state observable without creating an application lease. Room history is explicitly refused. Still 🟡 because live message/history transport remains post-M0-R and no independently operated discovery/publication/evidence exchange exists |
| Durable local delivery, application and read state | ✅ | `pkg/eventlog` | — |
| DeliveryAck and ApplicationAck payload codecs | ✅ | `pkg/payload` | — |
| StoredAck Relay protocol | ✅ | `pkg/mailbox`, `internal/vectors` | Strict signed codec, canonical vector, adversarial decode corpus, durable issuance, and tamper tests |
| Optional ReadAck wire profile | ✅ | `pkg/payload.ReadAck`, `read.ack` | Strict optional codec names the exact Event ID and user-facing read time; it remains distinct from delivery, application acceptance, authority, and TOS Receipt semantics |
| Encrypted offline Mailbox Relay | 🟡 | `pkg/mailbox`, `pkg/mailboxapi`, `cmd/tos-mailboxd`, [`MAILBOX_AUTHENTICATION.md`](MAILBOX_AUTHENTICATION.md), `internal/vectors` | Crash-safe opaque storage, scoped capabilities, finalized delegation rechecks, strict 2 MiB request/16 MiB response service frames, an eight-envelope read ceiling, private Unix listener/client, durable nonce claims, restart replay refusal, vectors and adversarial cases are implemented. The post-M0-R public transport binding and independent operator evidence remain |
| Multi-Relay redundancy and failover | 🟡 | `pkg/mailbox.StoreRedundant`, `pkg/mailboxapi.DepositClient` | Distinct pinned Relay identities and exact signed ACK thresholds work across separate service listeners; tests prove 2-of-2, explicit 1-of-2 operation after one listener disappears, and refusal of an unmet 2-of-2 threshold. Live independently operated Relay failover evidence is missing |
| Private group encryption and membership epochs | 🟡 | `pkg/room`, `pkg/group`, `rust/openmls-driver`, `pkg/eventlog`, `pkg/admission`, [`ROOM_ROLE_POLICY.md`](ROOM_ROLE_POLICY.md), [`ROOM_MODERATION.md`](ROOM_MODERATION.md), [`ROOM_HISTORY_POLICY.md`](ROOM_HISTORY_POLICY.md), `room.*` payload shapes | Logical membership, one current serializer, bounded epoch-bound roles, join-forward-only history, and auditable `hide`/`restore` moderation are enforced durably. Production admission (`4541a19`) re-verifies finalized authority and applies the moderation revision before queue publication; OpenFox `35bdac89` consumes it as a non-model control, persists a crash-safe presentation overlay, retracts already-applied history and action authority, and cancels an active room turn on hide. OpenMLS `0.8.1` owns RFC 9420 suite `0x0001`; the bounded sidecar and Go controller implement and persist the full tested local lifecycle. New and replacement leaves cannot decrypt prior ciphertext; direct-device history refuses room routes and room Event imports. The local OpenFox proxy additionally carries a target-private, Commit-digest-bound removal control through the opaque Relay, enforces its bootstrap controller, makes the control an fsynced batch boundary, hides it from the Agent transcript, and retains v1 message-only state without inventing migration authority. Restart, secrecy, PCS, tamper, replay, rollback, removed-role, moderation-revision, damaged-state, and substitution cases fail closed. RoomRecord v2 has no authority snapshot and therefore requires room resynchronization when upgrading to fail-closed v3. **Named gaps:** authenticated independently operated real Relay evidence; independent review and second implementation |
| Public Agent channels over Overlay with history synchronization | 🟡 | `pkg/publicchannel`, `cmd/tos-public-channeld`, [`PUBLIC_CHANNEL_CANDIDATE.md`](PUBLIC_CHANNEL_CANDIDATE.md) | The route-neutral candidate now has a network/authority-derived channel ID, finalized Endpoint publisher/moderator profiles with digest-linked adjacent succession/fork detection, independently signed content-addressed Events, strict per-publisher sequences plus cross-publisher causal parents, deterministic set convergence, immutable moderation projection, exact missing-ID repair and bounded strict JSON. Its single-writer local ledger persists immutable profiles, Events and canonical history manifests before atomic checkpoints, refuses profile/history rollback and forks, and re-verifies committed state after restart; corruption, orphan objects and concurrent writers fail closed in race tests. Deterministic positive vectors freeze signing bytes, IDs and the convergent head alongside decode- and verify-layer adversarial mutations. Bounded strict fetch request/responses walk untrusted tips and exact causal IDs, independently verify returned publishers, treat unavailability as retryable, merge replay idempotently, refuse stalled false heads, and require the complete Event set to reproduce the head before commit. `NativeNode` now keeps one fail-atomic incremental cursor instead of rebuilding and sorting the full known set per response. The reproducible single-core calibration covers verification, cursor discovery and Storage snapshot export/load through the 65,536-Event protocol bound; cursor discovery at that maximum took about one second, while durable snapshot export/load took about 46/13.3 seconds on the recorded host. A per-attempt guard caps authenticated peers, distinct heads, fetches, bytes and unavailable results; distinct-peer support prioritizes work but never grants validity, and no unmeasured PoW/economic rule is introduced. The pinned native `tosutils-go` carrier broadcasts live heads/Events over authenticated TOS ADNL Overlay and fetches exact history through RLDP, with independent application-authority verification, quotas and replay suppression. `NativeNode` and `tos-public-channeld` publish a separate ADNL transport identity/address plus signed node through native TOS DHT, discover a bounded authenticated peer set, assemble per-peer carriers, automatically reproduce and commit remote history, and restore it across full Gateway/node/store restart. The channel digest is correctly treated as the `pub.overlay` key and its TL-boxed hash as the wire/DHT Overlay short ID. The optional Sites path now exports a deterministic independently re-verifiable snapshot, invokes bounded no-shell `storage-daemon-cli`, persists a restart-idempotent Bag receipt, and propagates an authority-free BagID hint over Overlay without letting it consume head validity or replay budget. `SitesCatchUp` and `StorageCLIDownloader` add the bounded stock `add-by-hash`/`get` consumer, strict Bag/root/directory status binding, finalized snapshot re-verification, durable locator receipts, crash-window recovery, alternate-Bag refusal and restart no-redownload. One real two-node UDP test covers discovery, RLDP synchronization, injected Bag publication/hint propagation and same-identity node/store/mirror restart; another gives the sender no RLDP history and proves Overlay hint → injected download → verified commit → node/store/catch-up restart. Process-adapter tests cover exact publication and download CLI invocation. The opt-in `make test-storage-live` acceptance additionally starts two real same-host Storage daemons and a locally signed DHT, publishes on A, downloads/re-verifies on B, and proves offline receipt replay; it also exposed and fixed stock uppercase BagID/trailing-directory-slash representations. Still missing: representative-device/concurrent-peer production calibration, live independently operated/public-network DHT/Storage evidence, independent vector consumption/review and second implementation |
| Messenger-specific encrypted attachment protocol | 🟡 | `pkg/attachments`, `pkg/attachmentapi`, `pkg/attachmentadmission`, `pkg/attachmentops`, `cmd/tos-attachmentd`, `cmd/tos-attachment-text-scanner`, `artifact.encrypted`, [`ENCRYPTED_ATTACHMENTS.md`](ENCRYPTED_ATTACHMENTS.md) | Route-neutral AES-256-GCM chunks, secret E2EE Reference, ordered content addresses, strict Event binding, expiry/resume policy, vectors, and a private crash-safe opaque store exist. Endpoint-signed per-manifest Ed25519 capabilities separate upload/fetch/delete, bind exact operation bodies and short-lived nonces, recheck finalized delegation on every operation, and durably refuse replay across restart. V3 adds the missing E2EE-carried fetch-only recipient grant/key and binds it to sender/network/manifest/chunks/bytes/retention while retaining explicit read-only v1/v2 decoders. Daemon config v8 and local API v6 reserve the secret inbound Event from the general runtime, fetch and run `OpenForAgent` under its durable lease, and release only bounded admitted `text/plain` plus exact content/scanner evidence; OpenFox independently rehashes and persists before completion. The optional pinned `systemd-run` profile adds one transient cgroup per scan with hard memory, zero-swap, task, core and whole-group lifetime ceilings; launcher substitution, unavailable user-systemd authority, OOM and timeout fail before plaintext release. The daemon-owned outbound transaction immediately AES-GCM-encrypts sequential OpenFox MediaStore chunks, persists only ciphertext and restartable digest state, obtains distinct externally Endpoint-signed upload/fetch grants, resumes one storage chunk per call, and queues the exact prepared Event only after the final verified StoredAck. Prepared-only crash recovery, exact completed retry, authority substitution, maximum 512 MiB shape, real authenticated storage and race/fuzz tests cover the boundary. Still missing: independently operated public-TLS/live interrupted-transfer evidence, independently audited remote retention behavior, a selected production malware scanner and hostile corpus, independent hard-isolation review/non-text product evidence, and commercial storage terms (Expansion-Gate locked) |
| OpenFox `tos-messenger` channel adapter | 🟡 | `pkg/labgroup`, `pkg/mlslab`, [`OPENFOX_LAB_GROUP.md`](OPENFOX_LAB_GROUP.md), OpenFox `pkg/channels/tosmessengerlab`, `cmd/openfox-messenger-lab-agent` (`ed1fcc75`, `c47a98e0`), and production `pkg/channels/tosmessenger` (`aa6fe1f7`, `7fd3ac11`) | The lab channel supplies group IDs, durable cursors, three independent OpenMLS state owners, sequential third-member invitation, controller-only cryptographic removal, opaque Relay fan-out, tamper/refusal and full-process restart. Removal identity is encrypted, bound to the exact Commit digest and omitted from the Agent transcript; active peers continue after restart while the removed process is refused even when the Relay still delivers later ciphertext. The process runner owns one OpenFox channel per OS process with mode-`0600` transcripts/control sockets, caller-stable retry IDs, bounded catch-up triggers, and fail-visible errors. Its AgentLoop mode now gives each process a distinct mode-`0700` workspace, runs actual routing/session/model response publication with a local deterministic provider, holds lab cursor advancement through stable reply and transcript persistence, and skips the second turn on exact completed replay. Deployed Alice/Bob/Carol evidence proves one opening, exactly two Event-bound replies, restart-stable retry and Relay plaintext exclusion. This is local process/restart evidence, not independent network evidence. The production channel uses daemon application leases, strict local framing, independent current Event-v2 ID/canonical direct/room/moderation verification and typed authenticated provenance. Ordinary leases complete only after fsynced Event-ID application in the Agent session; exact restart replay is model-idempotent, substitution/non-durable history fail closed, busy sessions remain retryable, and hard abort retains acknowledged input. The actual AgentLoop carries runtime-owned origin through model execution and binds the final outbound reply to the current authenticated Event. Authenticated replies use exact configured routes and local API v6 daemon-owned construction. `SendMedia` now accepts only registered regular MediaStore files, independently hashes the exact open descriptor, streams 1 MiB sequential chunks, binds stable per-part idempotency and optional canonical-caption reply causality, and receives only upload progress or the canonical Event ID; storage/retention/keys/locator/network/Event authority remain in Messenger. Optional attachment polling receives only daemon-admitted text/scanner evidence and independently rehashes it before the same persistence/completion boundary. Moderation leases complete only after the OpenFox overlay is durable. Post-M0-R transport binding and independent real-network evidence remain open |
| Agent Packet carriage and Execution-Gate bridge | 🟡 | `agent.packet`, `pkg/agentpacketbridge`, durable Agent Packet records in `pkg/eventlog`, [`AGENT_PACKET_BRIDGE.md`](AGENT_PACKET_BRIDGE.md); `tos-ai` `a3c06d5`/`a9928de` supply the three-transport matrix; Messenger `41f53ab`/`cb97f0d` and OpenFox `cbe9f51c` supply the typed local receiver and daemon assembly | Exact signed bytes are carried under E2EE, verified against finalized controllers, bound to the authenticated Event sender/live recipient, durably nonce-deduplicated, and retried from pending receiver state. Concurrent and ordered A2A/MCP/Agent Packet delivery proves one shared Gate permits one execution. The canonical Packet crosses a bounded proxy-free Unix client into a mode-`0600` OpenFox provider socket, where authority is independently reverified before that Gate. Daemon-v6 admitted-event leases retry provider failure, survive restart without re-execution, and atomically exclude packets from runtime listing/direct claim; packet bytes never become model text. Still missing: selected live inbound transport and independent operation |
| First-contact admission policy and sybil resistance | ✅ | `pkg/admission`, `pkg/eventlog` (`AdmissionInviteRecord`), `pkg/localapi`, `pkg/mailbox`, `pkg/daemon`, and `cmd/tos-messenger-owner` | The v1 default is implemented: known-contact allow-list, 256-bit one-time invite introduction, and owner hold otherwise. Invite plaintext never lands on disk; an optional Agent scope and expiry are owner-signed, the first authenticated Event ID is claimed durably, exact retry remains idempotent after restart, Relay deposits bind the opaque token, and direct/Relay parity plus malformed/spent/substitution cases are tested. Daemon config v8 retains the v5 private-roster/bounds contract and refuses startup unless the public rule digest matches the finalized local delegation. PoW remains excluded without abuse evidence and Inbox Bonds remain Expansion-Gate locked |
| Relay, attachment, history, and inbox-bond commercial profiles | 🔒 | — | Expansion Gate |
| Cross-implementation positive vectors and adversarial corpus | 🟡 | `internal/vectors`, `pkg/conformance`, `cmd/tos-vector-report`, `pkg/evidence` | Positive vectors and a self-verifying adversarial corpus exist at object, verify, StoredAck, and concrete E2EE layers. The deterministic evidence bundle now hands their exact hashes to an external consumer, and the strict signed report verifier proves which implementation key claimed to consume them. **Named gap:** no independent implementation has returned a qualifying report — the harness makes that evidence reproducible but cannot manufacture independence |
| Independent multi-operator interoperability evidence | ⬜ | — | Needs a second implementation |

The M0-R in-process RLDP interruption now binds evidence to the current
transfer rather than polling client-wide cumulative statistics: it arms before
the one `DoQuery`, requires an exact complete-part boundary, and starts the
window by dropping the first inbound symbol of the following part. The sender
cannot emit that symbol until the prior part was decoded and acknowledged, and
success still requires counted suppression plus completion of the original
query. This removes a sequence-dependent zero-loss false measurement found by
repeated full-package testing; it does not add a single real-network sample or
change the row's 🟡 status.

## Scenario acceptance — what "two agents can talk" actually requires

The component table above tracks parts; a Messenger is accepted by scenarios.
Neither scenario below is close-able by any single component: each names the
exact chain still standing between the parts and a conversation, so progress
on the chain is visible and nothing is quietly assumed. A scenario is ✅ only
when the whole flow runs end to end on a real network between independently
operated agents.

| Scenario | Status | What already stands | The blocking chain, in dependency order |
|---|---:|---|---|
| **S1 — Two OpenFox agents discover each other and hold a one-to-one conversation** | 🟡 | Agent/Endpoint/Device identity; daemon-configured finalized delegation/policy bootstrap through production DHT+HTTPS refresh into durable device state; the approved 1:1 E2EE construction and its vectors; envelopes, payload codecs, durable inbox/outbox with delivery leases; negotiation-to-settlement commerce riding the same events; and an authenticated production OpenFox receive/reply boundary with daemon-owned outbound construction | (1) live independently operated discovery evidence; (2) the multi-operator M0-R study produces a route finding; (3) the production transport that finding selects, over the native stack; (4) independent E2EE review and second-language vector evidence; (5) bind the implemented outbound seam to that route and prove independent delivery/recovery |
| **S2 — Three OpenFox agents converse in a private room, third member invited by epoch transition** | ⬜ | Everything S1 stands on, plus: membership epochs with durable rollback/gap and single-authority enforcement; signed adjacent-epoch authority transfer; canonical MLS BasicCredential/group-id candidate bytes; selected MLS 1.0/OpenMLS suite `0x0001`; two-clock group adapter; authenticated route-neutral Mailbox storage; and a local acceptance loop in which genuine KeyPackage/Welcome/Commit transitions invite members. OpenFox `ed1fcc75` further proves three separately supervised Agent processes exchange MLS ciphertext through an opaque Relay, produce exactly-once replies bound to the opening Event, and preserve stable identity/retry state across restart | Everything S1 still lacks, plus: (6) independent OpenMLS Driver review/second implementation; (7) bind the demonstrated opaque delivery seam to authenticated real Relay retrieval after M0-R |

The chain is deliberately honest about blocker type: M0-R and independent
evidence need external operators/reviewers; the remaining links are buildable
code once their predecessors land. None of the listed architecture choices is
still waiting for an owner selection.

## Private-room cryptography selection — TOS-MLS v1 candidate

**Construction decision (2026-08-20): private rooms use MLS 1.0 (RFC 9420 / TreeKEM).** MLS versus a TOS-specific group ratchet is no longer an open choice. The adaptation below is the profile to implement and test; its wire commitments are not frozen until the canonical encodings, implementation, adversarial vectors, independent review, and second-implementation evidence exist.

### Why MLS is the first-principles fit

The Messenger needs all of these properties at the same time: asynchronous/offline members, groups from a few members to hundreds or thousands, one ciphertext per application message rather than O(n) pairwise fan-out, immediate exclusion after a removal, no history for a joiner, forward secrecy, post-compromise recovery, multi-device revocation, and operation through untrusted/multiple Mailbox Relays. MLS was designed for asynchronous group key establishment and gives tree-based logarithmic membership updates with a single application ciphertext. RFC 9420 explicitly targets groups whose members need not be online at the same time and provides forward secrecy and post-compromise security. Sender-key designs are cheaper initially but require pairwise/out-of-band redistribution on membership changes and provide materially weaker compromise recovery; pairwise fan-out makes every group event O(number of recipient devices) in bandwidth and Relay storage; a single shared room key makes removal, compromise recovery and device revocation depend on a trusted distributor. Those tradeoffs contradict the Messenger's decentralized Agent model.

TOS does **not** delegate room authority to MLS. MLS answers “which cryptographic clients can read this epoch”; the selected single current-authority Agent policy in `pkg/room` answers “which Agents/devices are allowed to become those clients.” A Relay is only opaque delivery/storage and is never the source of truth for membership.

### Selected TOS-MLS v1 profile candidate — not frozen

1. **Protocol and cipher suite:** MLS 1.0 / RFC 9420. The only v1 cipher suite is `0x0001`, `MLS_128_DHKEMX25519_AES128GCM_SHA256_Ed25519` (X25519 HPKE, HKDF-SHA-256, AES-128-GCM, Ed25519). It is the MLS 1.0 mandatory-to-implement suite, matches the Messenger's Ed25519 identity ecosystem, and maximises independent-library interoperability. No per-room cipher-suite negotiation is allowed in v1; another suite requires a Messenger protocol-version change or MLS `reinit`, preventing silent downgrade and ecosystem fragmentation.
2. **One device, one MLS leaf:** every currently authorised TOS Device/Endpoint instance is an independent MLS client/leaf. Devices never share MLS leaf private state or a LeafNode signature private key. Revoking one device removes that leaf without forcing the Agent's other devices out. Adding an Agent adds the current authorised device KeyPackages for that Agent; later device succession becomes MLS Add/Remove/Update work.
3. **TOS identity binding:** use the standard MLS BasicCredential form for library interoperability. Each leaf has a distinct Ed25519 LeafNode signature key; it must not reuse the delegated Endpoint key or a key shared by the Agent's other devices. A new endpoint-signed MLS device credential binds that leaf key and its KeyPackage to `(network, agent_id, endpoint_id, device_id, current device-set/delegation commitment, validity window)`. The application verifies this authority against finalized delegation and current device state before accepting a KeyPackage, Add or Commit. `pkg/group` now specifies the exact raw-genesis BasicCredential identity, v2 credential preimage, network-bound group ID and candidate vectors; independent consumption and cryptographic review still gate freeze. A syntactically valid MLS credential is never authority by itself.
4. **Two clocks, not one:** `room_epoch` is the logical Agent-membership epoch already owned by `pkg/room`; `mls_epoch` is the cryptographic MLS group epoch. A logical Agent add/remove advances `room_epoch` and necessarily advances `mls_epoch`, while device succession, leaf updates and PCS refreshes may advance `mls_epoch` with the same `room_epoch`. The current `pkg/group` one-room-epoch/one-secret assumption must therefore change rather than forcing MLS into the wrong model.
5. **Room/network binding:** derive the MLS group identifier from the TOS network identity plus `room_id` under a new Messenger domain separator, so an identical room on another network is another MLS group. The preimage uses raw 32-byte genesis hashes; strict JSON uses lowercase bare hex. The separator, schema/domain version, and positive/adversarial vectors must be committed before freeze; an existing canonical preimage is never silently extended. Before accepting any membership-changing MLS commit, the application verifies the resulting leaves against the authorised TOS room membership and current device sets; cross-network, stale-room, revoked-device and foreign-Agent commits fail closed.
6. **Untrusted multi-Relay delivery:** KeyPackages, Welcome messages, proposals, commits and MLS PrivateMessages may be carried/stored by any selected Mailbox Relay. Relays cannot decrypt and cannot authorise membership. Missing MLS epochs are a refresh-state condition: fetch the missing commit chain and apply it in order; never guess or skip an epoch.
7. **Commit serialization is an application rule:** MLS deliberately does not turn an untrusted Delivery Service into consensus. One current authority Agent serializes membership. The creator starts as authority; transfer requires the current authority's signature on a single-step room-epoch transition. Membership-changing commits must come from that authority and chain to the single locally accepted parent; concurrent/conflicting commits are refused and Relay arrival order is never authority.
8. **Application messages:** normal private-room events are MLS `PrivateMessage` application data. The outer Messenger envelope/event ID keeps routing, dedupe, causal parents, typed payloads and durable delivery; MLS owns group confidentiality/authentication and epoch key evolution. Delivery/Application/Read ACKs do not advance MLS epochs.
9. **Recovery and persistence:** persist MLS state, consumed KeyPackages, Welcome processing, accepted commit parent and epoch state so restart cannot reuse one-time KeyPackages, resurrect a removed leaf, or accept two children for one parent. Recovery fails closed on an MLS epoch gap, rollback or TOS-membership mismatch.
10. **Post-quantum migration:** do not invent a hybrid MLS construction in v1. When an interoperable, reviewed PQ/hybrid MLS cipher suite exists, migration is an explicit Messenger version / MLS `reinit` event with new vectors. Algorithm agility must not become downgrade agility.

### Implementation / acceptance plan

TOS-MLS v1 remains 🟡 until all of these are true:

- ✅ application adapter uses explicit `room_epoch` + `mls_epoch`; the legacy construction contract remains as the founding/join/removal refutation floor;
- ✅ integrate pinned OpenMLS `0.8.1` behind a bounded process-isolated Messenger Driver instead of implementing TreeKEM/HPKE from scratch; independent review of this boundary remains a freeze gate;
- ✅ endpoint-signed per-device MLS credential and strict KeyPackage publication codec exist; every leaf key is distinct and Endpoint-key reuse is refused;
- ✅ specify the BasicCredential identity and group-id canonical bytes using raw 32-byte canonical genesis hashes, register every new domain/schema version, and commit positive plus adversarial candidate vectors (independent consumption still gates freeze);
- ✅ bind credentials to current TOS Agent → Endpoint → Device authority and reject revoked/absent, stale, expired, foreign-network, foreign-Agent and wrong-key leaves;
- ✅ convert device succession to MLS Add/Remove/Update, including revoking one device while retaining the Agent's other device;
- ✅ persist opaque MLS state, globally consumed KeyPackages, Welcome receipts and commit ancestry across restart; replay, duplicate-Welcome, state tamper, epoch-gap, rollback and concurrent-commit cases fail closed;
- ✅ enforce one durable current authority for membership changes and a current-Endpoint-signed, adjacent-epoch transfer verified against both finalized delegations;
- ✅ run `pkg/group/conformance` plus MLS-specific vectors for founding agreement, joiner-no-past, removed-member-no-future, forged commit, stale membership digest, wrong network/device set, exporter/secret separation and PCS after an update;
- ✅ bound v1 private rooms to 32 Agents, 64 active leaves, 16 devices per Agent and 32 leaf operations per Commit, with fail-closed capacity tests;
- ✅ demonstrate one encrypted application event delivered through multiple untrusted Relays to all current devices, including offline catch-up over several MLS epochs, without any Relay learning group secrets or deciding membership (`pkg/mlslab` composes two independently keyed durable Mailbox stores; real independently operated network evidence remains open);
- cross-check the selected TOS-MLS profile against a second independent MLS implementation before the group wire profile is called frozen.

## The policy engine, split honestly

"Agent message policy engine and prompt-injection firewall" is not one row,
because parts of it are closed and parts cannot be closed from inside this
repository, and one ✅ or one 🟡 would misstate both halves.

| Piece | Status | Note |
|---|---:|---|
| Action policy evaluator (effects, ceilings, provenance typing) | ✅ | `pkg/firewall` |
| Authenticated owner decision queue (challenge + signature, bounded, expiring) | ✅ | `pkg/localapi`, `pkg/eventlog`, and `cmd/tos-messenger-owner` (`0541723`): pending actions can be rendered online, bound to a single-use challenge, signed offline or by a replaceable isolated signer, and submitted without exposing the private key to the Agent runtime |
| One-shot side-effect authorization | ✅ | Owner-granted actions and policy-allowed **spends and tool calls** become durable grants consumed by `actions.claim` exactly once. Tool calls require a canonical runtime-supplied `idem_` key committed into the Action ID: a retry reproduces the spent grant, while two legitimate identical invocations use distinct keys. Low-risk message/local effects remain deliberately inline |
| OpenFox provenance enforcement | ✅ | OpenFox `fbb052df` derives provenance from runtime-owned inbound/session metadata, persists it outside model-visible provider payloads, and binds complete durable context to tool proposals; legacy/unattributed/lossy/oversized lineage fails closed. OpenFox `7fe6ec10` closes the ingress half: only a daemon-claimed event whose metadata, content-addressed Event ID, and canonical text independently verify receives typed authenticated origin. The plaintext lab channel deliberately never does |
| Wallet/tool adapter enforcement | ✅ | OpenFox `fbb052df` classifies built-in, MCP, hardware, skill, messaging, and sub-Agent tools and checks the daemon before `Execute`; unknown injected tools fail closed, retries use runtime-derived keys, owner holds are polled, and grants are claimed once. Its `AuthorizedCustodySigner` commits exact quote/asset/mandate terms before funding and separately protects settlement key use; `4736f2c7` makes the authorizer and mandate mandatory in `NewNativeBuyer` and installs the wrapper itself |

## Commerce, split honestly

| Piece | Status | Note |
|---|---:|---|
| Chain-native money, complete terms, mandate and approval binding | ✅ | `pkg/negotiation` |
| Durable mandates, budgets, negotiations | ✅ | Every transition persists; crash windows between the budget ledger and a negotiation snapshot reconcile deterministically at journal open |
| Finalized-quote verification contract and term matching | ✅ | `negotiation.QuoteResolver`, `VerifiedAcceptedQuote` |
| Concrete finalized-state resolver and daemon wiring | 🟡 | The **local Agent/delegation startup path is wired**: daemon config names 3–8 chain authorities, a strict-majority quorum, bounded client policy, an explicit Native Registry version, a daemon-owned rollback checkpoint, and the exact delegation file. Production `daemon.Open` builds the upstream `toschain` + `nativecore` resolver through `pkg/chainagent`, verifies the delegation against finalized live Agent state, requires its Agent/endpoint to equal the configured installation before opening either socket, and installs its outbound event-class grant in the dispatcher. The Quote half has a concrete `pkg/chainquote` finalized resolver assembled by optional daemon-v7 escrow code/checkpoint settings. Digest-only negotiation can use the owner-attested daemon-journal `EscrowLocator`; funded execution instead supplies its deterministic escrow address to read-only `quotes.verify`, which accepts it only after the code-authenticated finalized account holds the commitment and all expected terms plus the complete local network match. It never persists the runtime locator. Exact owner retries remain idempotent, redirects conflict, damaged records fail closed, and OpenFox blocks task dispatch until the direct read succeeds—including funding-lease recovery. **Named gap:** no live-node buyer/provider settlement run has supplied deployment evidence. This keeps the combined row 🟡 |
| Negative-case resolver tests (wrong network/registry/account/provider/money/escrow) | 🟡 | The Messenger's term-matching half rejects divergence in amount, capability, version, expiry, provider, manifest, or **escrow**; a quote in a **foreign asset at the same nominal amount** is rejected; a missing, unfinalized, wrong-commitment, undecodable, or invalid mapped quote is refused; a transient resolver **error** leaves negotiation retryable. The concrete quote bridge confirms the located finalized escrow holds the requested commitment, while upstream `toschain` owns network/account/code/finality checks. **Named gap:** no live-node composition test has exercised those layers together |

## Next, in the order the architecture allows

1. **Run the study.** Both collectors exist; what is missing is evidence — ≥3
   operators, ≥3 sites per required scenario, real networks. The report tool
   refuses to bless anything less as a route decision. A single-operator dry
   run of the whole chain is scripted in
   [`M0R_PILOT_RUNBOOK.md`](M0R_PILOT_RUNBOOK.md). *(Blocked: needs external
   operators.)*
2. **The adversarial corpus.** *(Done at both the decode and verify layers,
   including the reachability trial layer. The only remaining gap is that no
   second implementation exists to check against — that is item 4's blocker,
   not a corpus gap.)*
3. **Membership epochs for rooms.** *(Done: `pkg/room` state machine +
   durable epoch ledger. TOS-MLS v1 is now selected for the cryptographic half;
   implementation is item 5.)*
4. **Independent multi-operator interoperability evidence.** *(Blocked: needs a
   second implementation and the multi-operator study.)*
5. **Implement the TOS-MLS v1 private-room profile candidate.** The construction
   decision is closed: MLS 1.0 / RFC 9420. Cipher suite `0x0001`, one device per
   leaf, two clocks (`room_epoch` and `mls_epoch`), untrusted Relay carriage,
   and TOS identity/device validation are selected profile details. The
   application adapter, device leaf-key authority, candidate vectors, durable
   recovery, and device succession wiring are implemented. Integrate a reviewed
   MLS library and run the remaining cryptographic/Relay acceptance plan above.
   Enforce the selected single current-authority Agent rule and signed transfer;
   MLS and Relay order must never substitute for that application authority.

Item 1 requires external study operators. The canonical genesis representation
is settled and applied to the audited candidate preimages; room authority is a
settled choice whose remaining row is implementation and evidence work.
